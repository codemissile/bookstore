import React from 'react';
import ReactDOM from 'react-dom';
import './main.css';  // Optional, can be removed if you don't have styles yet
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
