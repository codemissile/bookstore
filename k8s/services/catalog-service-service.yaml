apiVersion: v1
kind: Service
metadata:
  name: catalog-service
spec:
  selector:
    app: catalog-service
    version: blue  # Switch to blue when success
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
